/// <summary>
/// Lose Or Win.
/// Description of Class - If player Loses or Wins then restart count at 1.
/// </summary>

using UnityEngine;
using System.Collections;

public class LoseOrWin : MonoBehaviour {

	// Use this for initialization
	void Start () {
		//restart count at 1
		OnStart.restartCount= 1;
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
